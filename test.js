//  NODE REPL 모드
// Read / Eval / Print / Loop
// 사용자가 입력한 내용을 읽고 -> 그결괏값을 구한다음 -> 결괏값을 출력 -> 이런과정을 계속 반복
// In >> node
// exit >> .exit

console.log("hello node.js");

// module -> js 파일 중 하나 (전체를 이루는 파일 중 하나)

function add(a, b)